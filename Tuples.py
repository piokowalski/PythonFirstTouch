# tuple example (they're immutable)

coordinates = (5,8)

print(coordinates[0])
#printing value of index 0

# coordinates[1] = 9
# print(coordinates) #I told You, immutable ;)! Created once, ready to use

